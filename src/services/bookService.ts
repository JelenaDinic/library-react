// import { myAxios } from './api.service'

// export const getBooks = (): Promise<BookResponse[]> => myAxios.get('Books')

// export const postBook = (newBook: Book): Promise<BookResponse> => {
//     return myAxios.post('Books', newBook)
// }

const bookService = {
  // get: (): Promise<BookResponse[]> => myAxios.get('Books'),
  // post: (newBook: Book): Promise<BookResponse> => {
  //     return myAxios.post('Books', newBook)
  // }
}

export default bookService
